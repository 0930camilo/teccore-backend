spring.application.name=teccore-backend


# Base de datos local
spring.datasource.url=jdbc:mysql://localhost:3306/tecCore
spring.datasource.username=root
spring.datasource.password=Allweneedislo.12

#Base de datos TiDB Cloud (conexión directa)
#spring.datasource.url=jdbc:mysql://gateway01.us-east-1.prod.aws.tidbcloud.com:4000/sigivData?sslMode=VERIFY_IDENTITY
#spring.datasource.username=2Nzm2w49W2Cztnu.root
#spring.datasource.password=DXySeoiOXIiwA8B9

#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USER}
#spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver



# Pool de conexiones (HikariCP)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.boot.allow_jdbc_metadata_access=true
spring.jpa.open-in-view=true
spring.jpa.show-sql=true

# Servidor
server.port=${PORT:8080}
spring.mvc.pathmatch.matching-strategy=ant-path-matcher

# Logs
logging.level.org.springframework.security=DEBUG

# Correo
mail.default.host=${MAIL_DEFAULT_HOST:smtp.gmail.com}
mail.default.port=${MAIL_DEFAULT_PORT:587}
mail.default.starttls=${MAIL_DEFAULT_STARTTLS:true}

# Seguridad JWT
security.jwt.secret=${JWT_SECRET:change-this-secret-key-change-this-secret-key}
security.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# CORS
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173,http://localhost:4200}

